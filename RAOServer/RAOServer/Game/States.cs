namespace RAOServer.Game {
    public enum PlayerStates {
        PlayerLobby,
        PlayerGame
    }

    public enum RoomStates {
        RoomWaiting,
        RoomPlaying,
        RoomOver
    }
}